# Core Maintainers Meeting - 2026-04-02

## Attendees

- @benbrandt
- @nikomatsakis
- @anna239
- @agu-z
- @ignatov

## Agenda

- RFDs
  - Move to preview:
    - session/resume
    - session/close
  - In Progress work on RFDs
    - Auth Methods
      - Claude ACP - Implemented
      - Codex ACP - Implemented
      - Zed - in-progress (terminal done, still need env var)
      - JetBrains - in-progress (terminal done, still need env var)
    - Elicitation
      - Originally: just session
      - New proposal: Global (i.e. request), as well as session + tool id
    - logout
    - additional directories
      - Claude + Codex allow for passing in extra ones at new/load time
      - But they don't persist this, so not in session/list
    - boolean config options
    - Language model providers
    - session/delete
      - Not really claude / codex
    - NES (Next Edit Suggestion)
      - First draft of the schema + TS SDK
      - for v2 of the protocol: if we are doing NES, it might be nice to have the agent say it _can't_ do normal agent sessions and only NES
        - Or we could have a wrapper that just uses this model as an agent?
        - Maybe also reflect in the registry somehow
      - document/\* capabilities available for other agents?
    - Rust SDK
      - Pulled in all the latest crates
      - Niko and Ben to sync on migration plan
    - MCP over ACP
      - Some extensions to the protocol
      - adapter that sits on the other side rewriting it to an http remote for the agent
        - could happen in every agent, or in the SDKs, or a shared WASM binary (but requires localhost port)
    - Ability for agent to request client state
    - Better Plan mode
      - current one is just "todos"
      - need a way to support plan files
    - Better Subagent representation
    - Queueing + sidechannel messages

- Things to figure out:
  - image support needs to be somehow closer to models
    - capability update notification?
    - session capabilities?

## Action items

- [x] [@benbrandt] Set up folder on the website for blog posts
- [x] [@benbrandt] Draft of new notification-based prompt format
  - Message ID handling
  - queueing
- [ ] [@ignatov] JetBrains move Codex wrapper to ACP org for joint maintenance
- [ ] [@benbrandt] telemetry for used methods + update notifications
