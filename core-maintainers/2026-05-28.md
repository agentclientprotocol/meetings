# Core Maintainers Meeting - 2026-05-28

## Attendees

- @benbrandt
- @anna239
- @ignatov
- @nikomatsakis

## Agenda

- RFDs
  - Stabilized:
    - Logout
  - Preview:
    - Rust SDK
    - Additional directories
  - Shortlist to move to preview:
    - session/delete: what guarantees do we want to make with delete?
      - Are we ok if the only guarantee is that it isn't listed again in session/list?
      - Seems everyone is in agreement with the current state, can move to preview
        - Do we want to add a field for whether it is archived/deleted or not?
          - Can look into adding this later
    - $/cancel_request: Rust SDK PR open
    - Session usage:
      - Context notification seems fine
      - how do we want to handle turn cost/usage etc?
      - We should have the agent handle aggregation logic, and send a snapshot of current spend, rather than relying on the client to do it
        - Client could subtract from multiple snapshots
      - Context window + cost :thumbsup:
      - Token usage: might be better as a notification
    - Elicitation
      - More work needed on the tool call level
- v2
  - Ben added basic way for Agents at the SDK level to speak v2 and SDK backports to v1 for v1 clients
    - Will be making sure this still holds up as other changes land
  - Working on adding more of the other changes to the schema
  - FS + terminal client capabilities
    - Assumption in v1 was clients could have a better idea of file state + terminal setup
      - Terminal:
        - Claude + Codex are doing sandboxing, don't care about our terminal setup
      - FS:
        - Agents have to rework their tools in order to use this, often they don't (adapters can't use it)
        - If we want to keep it, we need to provide more FS methods (delete, binary files, mmtime etc)
    - We could focus on allowing ACP to specify a sandboxed environment rather than provide these capabities, could be a runtime around the agent
      - Allow clients to specify the setup rather than offer the capabilities for the agent to use
- Where is ACP succeeding?
  - Seems to be picking up adoption in large enterprises building tooling for their developers, and also programmatic usage around agents

## Action items

- [ ] [@benbrandt] Update v2 list with notes from today
